# Review Plan Eval Fixtures

These fixtures define the minimum quality regression set for the review-plan workflow.

Each fixture contains an input payload and expected criteria. The first pass is intentionally lightweight so the team can run it in normal unit tests without a separate eval platform.
