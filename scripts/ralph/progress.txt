# Ralph Progress Log
Started: 2026-05-06
---

Current PRD: mini program parent stability.

Scope:
- Primary product focus is the parent-facing WeChat mini program under miniprogram/miniprogram/.
- Main parent path: upload wrong question -> check wrongbook -> generate/download/open PDF.
- Main pages/helpers: parent-home, parent-upload, parent-wrongbook, parent-bind when setup requires invite-code binding, and parentApi.
- Bridge, Flask, Redis/RQ worker, and PDF/LaTeX renderer work is only in scope when required to prove the mini program parent path is stable end to end.
- Do not work on unrelated website React frontend, dashboard, account, weekly followup, lesson planning, payment, or classroom workflows.
- Do not reintroduce AI box selection or wrong-question-boxes.

Required reading before each story:
- AGENTS.md
- handoff.md
- miniprogram/handoff.md
- scripts/ralph/prd.json
- scripts/ralph/progress.txt
- docs/superpowers/specs/2026-05-06-miniprogram-ralph-stability-loop-design.md
- scripts/ralph/miniprogram_stability_loop_instructions.md

Baseline proof:
- scripts/ralph/miniprogram_stability_loop_proof.sh
- scripts/ralph/miniprogram_visual_acceptance_guardrail_proof.sh
- scripts/ralph/parent_upload_2_acceptance_guardrail_proof.sh
- scripts/ralph/miniprogram_upload_stability_proof.sh when upload behavior is touched
- scripts/ralph/production_upload_smoke_runbook_proof.sh when PDF, LaTeX, worker, or production smoke docs are touched
- python -m unittest tests.test_wrong_question_library_pdf tests.test_ai_processor_prompt -v when PDF or LaTeX is touched
- cd frontend && npx tsx --test src/wrong-question-latex.test.ts src/render-wrong-question-library-pdf.test.ts src/render-wrong-question-practice-sheet-pdf.test.ts when PDF or LaTeX is touched
- git diff --check

Run commands for the user:
- Check active queue: scripts/ralph/run_codex_ralph.sh --check
- Run one story: scripts/ralph/run_codex_ralph.sh 1
- Run up to all remaining stories: scripts/ralph/run_codex_ralph.sh 9

Status:
- Archived completed visual refresh PRD to scripts/ralph/archive/miniprogram_visual_refresh_prd_20260506.json.
- Archived completed visual refresh progress to scripts/ralph/archive/miniprogram_visual_refresh_progress_20260506.txt.
- Active PRD is mini program parent stability with nine MP-STABILITY stories.
- MP-STABILITY-001 passed: scripts/ralph/miniprogram_stability_loop_proof.sh now directly validates active PRD shape, nine MP-STABILITY stories, parent upload-to-wrongbook-to-PDF scope, stability instruction reference, and visual/upload/PDF/LaTeX/code-review gate coverage.
- MP-STABILITY-002 passed: parent-upload real-use interaction audit confirmed existing coverage for append/switch image, manual boxes, tiny/narrow resize, rotation, deletion, crop/export failure draft retention, submit retry, and interaction locks. Added a regression and fix so canceling camera/album selection is a stable no-op instead of showing a parent-visible upload failure. Updated visual/upload guardrails so archived visual/upload PRD pass gates can run as reusable baselines under the active MP-STABILITY queue.
- MP-STABILITY-003 passed: text/voice reason input remains optional; voice mode with no saved recording still falls back to text/image-only submit, recorder start/onError failures now clear stale "recording" UI state, and audio upload timeout/network/5xx failures keep the draft and recording retryable. The parent upload guardrail now explicitly requires these voice recovery fixtures.
- MP-STABILITY-004 passed: bridge upload proxy coverage already preserves website 202 accepted payloads with optional fields missing and maps 400, 413, 502, timeout, and malformed website acceptance responses into structured stable errors. Added parentApi coverage and fix for malformed bridge 2xx upload responses so parents get the existing draft-preserving retry message instead of raw non-JSON text; malformed 413 still uses the smaller-crop/retake guidance.
- MP-STABILITY-005 passed: parent-upload task polling now treats website `state=missing_record` as recoverable background work even when raw `status=ready`, so accepted uploads are not incorrectly marked complete or cleared before the wrongbook record exists. Stale pending tasks now show long-running recovery copy, parent-wrongbook surfaces missing-record upload progress, and the parent upload guardrail explicitly requires these fixtures.
- MP-STABILITY-006 passed: audited parent-wrongbook WXML/WXSS/JS/tests and parentApi wrongbook/PDF calls. Existing focused coverage confirms wrongbook refreshes upload task ids before records/PDF metadata, failed tasks do not show AI success copy, background and missing-record uploads stay visible with PDF-not-ready copy, missing `pdf_url` is recoverable, and `openWrongQuestionLibraryPdf` handles not-ready, download-failed, and open-failed states. No runtime code change was needed.
- MP-STABILITY-007 passed: audited the student wrong-question PDF/LaTeX path across `ai_processor.py`, `pdf_engine.py`, `frontend/src/wrongQuestionLatex.js`, browser library/practice PDF renderers, backend PDF tests, frontend renderer tests, and worker PDF failure coverage. Hardened indexed bare roots such as `\sqrt[3]{8}` so both frontend PDF HTML text fallback and ReportLab fallback render readable root text instead of leaking raw LaTeX. The proof also re-ran production smoke runbook checks, backend PDF/AI prompt tests, frontend LaTeX/PDF renderer tests, mini program visual/upload/stability guardrails, and `git diff --check`.
- MP-STABILITY-008 passed: audited parent-home, parent-upload, parent-wrongbook, and parent-bind source/structural visual contracts for narrow-screen action hierarchy, button density, safe-area rules, text clipping guards, scroll traps, and parent-facing developer leftovers. No WeChat DevTools, real-phone, video, or user screenshot evidence was available, so this is a local structural smoke only and does not claim true-device visual stability. Found one parent-facing technical leftover: parent-upload accepted-stage copy exposed raw task ids; removed that visible id and locked the behavior in the upload page test plus the visual acceptance guardrail.
- MP-STABILITY-009 passed: added `scripts/ralph/miniprogram_stability_acceptance_guardrail_proof.sh` as the final mini program stability guardrail. It verifies the active nine-story MP-STABILITY PRD, parent mini program upload-to-wrongbook-to-PDF scope, unrelated website/business workflow exclusions, all stories `passes=true` before final completion, required visual/upload/stability/PDF/LaTeX proof commands, and the remaining manual WeChat DevTools, real-phone, production Redis/RQ, and production PDF smoke checks.
- Final automated status: MP-STABILITY-001 through MP-STABILITY-009 are complete in local proof.
- Next story: <none>.

Remaining manual smoke commands/checks after final guardrail:
- Local final acceptance proof: `scripts/ralph/miniprogram_stability_acceptance_guardrail_proof.sh`
- Ralph queue check: `scripts/ralph/run_codex_ralph.sh --check`
- Production runbook command set: follow `docs/production-upload-pipeline-smoke-runbook.md`, especially `pm2 status xingrun xingrun-bridge xingrun-rq-worker`, `redis-cli -u "$XR_REDIS_URL" ping`, `.venv/bin/rq info --url "$XR_REDIS_URL" "$QUEUE"`, bridge health, task status, wrongbook metadata, and student PDF download checks.
- WeChat DevTools / real phone: capture narrow-screen evidence for parent-home, parent-upload, parent-wrongbook, parent-bind, upload retry states, wrongbook refresh, PDF download, and `wx.openDocument`.

Manual checks that cannot be honestly automated here:
- MP-STABILITY-002 still needs one WeChat DevTools or real narrow-phone upload-page visual smoke for choose image/camera, manual tiny/narrow box resize, rotate, delete, cancel picker, and submit button safe-area behavior; no true-device visual evidence was available in this local run.
- MP-STABILITY-003 still needs one WeChat DevTools or real-phone smoke for recorder permission denied/start failure, recorder runtime error, no-recording voice mode submit, and audio upload retry; local source tests are not true-device recording evidence.
- MP-STABILITY-006 still needs one WeChat DevTools or real narrow-phone smoke for wrongbook refresh, PDF-not-ready display, PDF download failure recovery, and `wx.openDocument` behavior; local source tests are not true-device PDF evidence.
- MP-STABILITY-007 still needs production Redis/RQ plus real PDF open smoke for a safe parent upload with malformed/indexed-root LaTeX; local renderer and fallback tests are not production browser/worker evidence.
- MP-STABILITY-008 still needs WeChat DevTools screenshots or real-phone screenshots/video for parent-home, parent-upload, parent-wrongbook, and parent-bind at a narrow phone size such as 360x800 or 375x667. Check touch target size, button density, wasted card space, text clipping, overlap, safe-area padding, scroll traps, and visible copy after a real upload reaches accepted/background/ready/failed states.
- At least one real narrow phone smoke for upload, wrongbook refresh, PDF download, and PDF open.
- Production Redis/RQ worker smoke with a safe parent test account and real task id.
- Production PDF open smoke after a real upload reaches ready.
