# Ralph Progress Log
Started: 2026-05-03
---

Current PRD: mini program parent upload 2.0 stability hardening.

Scope:
- WeChat mini program parent upload flow: select image, manual box, crop export, optional text/voice reason, submit, polling, recovery, wrongbook refresh, and PDF entry.
- Bridge upload proxy behavior under miniprogram/backend/ where it directly affects parent upload reliability.
- Website WeChat upload task API, queue/worker task semantics, wrong-question record preservation, and PDF refresh behavior where they directly affect parent upload reliability.
- Do not reintroduce AI box selection.
- Do not redesign unrelated mini program screens or add unrelated product features.
- Each iteration should complete one user story from scripts/ralph/prd.json.

Baseline proof for every completed story:
- scripts/ralph/miniprogram_upload_stability_proof.sh

Status:
- Archived the completed website frontend stability Ralph PRD and progress log under scripts/ralph/archive/.
- MP-UPLOAD-001 passes: added scripts/ralph/miniprogram_upload_stability_proof.sh as the shared mini program upload stability baseline proof runner.
- MP-UPLOAD-002 passes: parent upload now shows explicit stages for crop export, optional audio upload, image upload/task acceptance, recognition polling, background processing, ready, partial failure, and failed states. Draft-mutating upload controls are disabled only while submitting and release after failure/completion. Baseline proof is still scripts/ralph/miniprogram_upload_stability_proof.sh.
- MP-UPLOAD-003 passes: parent upload task polling no longer collapses the whole batch on one status request failure; malformed task payloads stay pending under the original task id, mixed ready/failed/pending batches keep polling pending tasks, and max-attempt exhaustion reports accepted uploads continuing in the background.
- MP-UPLOAD-004 passes: parent upload now persists accepted task ids under a scoped parent/child recovery key with child and image/box metadata, restores pending tasks on page reopen by polling status without re-uploading crops, clears recovered tasks only after ready/failed terminal states, and shows a recoverable message for corrupted/unavailable local storage.
- MP-UPLOAD-005 passes: manual crop and box interaction is covered for large portrait/landscape photos, rotated images, tiny boxes, switching images, deleting the selected box, export failures, and multiple-box export. Crop export failures now name the failing item without clearing the draft, selected image/box state stays stable after rotate/export failure/delete, and crop export locks conflicting gestures plus duplicate submit actions while preserving the existing 1280 x 1792 / quality 0.82 limits.
- MP-UPLOAD-006 passes: parentApi now uses scoped shorter timeouts for audio upload, compressed image task submission, and task status refresh. Oversized compressed images get a specific non-retryable draft-preserving message; transient 5xx, network, and timeout failures get retryable draft-preserving messages; the upload page keeps the original draft after a retryable failure and clears it only after a later accepted retry.
- MP-UPLOAD-007 passes: bridge upload proxy now preserves website accepted task payloads even when optional task fields are missing, maps website 400/413/502/timeout/malformed responses into structured retryable or final bridge responses, and parentApi keeps structured bridge error metadata for parent-facing handling.
- MP-UPLOAD-008 passes: website upload task API now returns stable task status fields for pending, stale pending, ready, failed, and missing-record states; task payloads include retryable/is_stale/record_status/record_missing, image-only submission remains valid, unauthorized task status access remains hidden, and enqueue failure persists a retryable failed task before returning 502.
- MP-UPLOAD-009 passes: worker/API/data coverage now preserves the original submitted image and teacher-visible record when recognition succeeds but student-library PDF rebuild fails; recognition/processing failures still keep an image-backed failed record. Failed task payloads now include concise parent_error_message and raw maintainer_error_detail, and the mini program upload summary prefers the concise parent-facing reason over low-level errors.
- MP-UPLOAD-010 passes: parent upload success now links directly to the child's wrongbook with accepted task ids preserved; parent wrongbook refreshes ready/failed/background upload task status before loading records, labels failed or still-processing cards without implying AI success, and surfaces PDF not-ready, missing pdf_url, download failure, and openDocument failure as recoverable parent-visible messages.
- MP-UPLOAD-011 passes: added docs/production-upload-pipeline-smoke-runbook.md and scripts/ralph/production_upload_smoke_runbook_proof.sh. The runbook gives read-only production checks for PM2 services, Redis, RQ worker, Flask, bridge, upload limits, sample task status, wrongbook/PDF metadata, and explicitly labeled PDF cache/repair mutations; troubleshooting now covers pending tasks, enqueue failures, 413s, and PDF refresh failures.
- Next story: MP-UPLOAD-012 Add parent upload 2.0 acceptance guardrail.
