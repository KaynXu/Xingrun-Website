# Ralph Progress Log
Started: 2026-05-03
---

Current PRD: mini program visual refresh and production polish.

Scope:
- WeChat mini program frontend only: miniprogram/miniprogram/.
- Main surfaces: parent-home, parent-bind, parent-upload, parent-wrongbook, PDF/progress entry points, and shared parent-facing visual states.
- Core goal: make the mini program look like a formal, polished education product instead of a prototype.
- Button placement and action arrangement are core requirements, not incidental styling.
- Do not work on website React frontend under frontend/.
- Do not change bridge/backend/API behavior unless the user explicitly approves a separate non-visual change.
- Do not reintroduce AI box selection.
- Each iteration should complete exactly one user story from scripts/ralph/prd.json.

Visual direction:
- Warm, clean, trustworthy, mobile-first education product.
- Clear hierarchy: page title, context, content, primary action, secondary actions, state feedback.
- One clear dominant primary action per section.
- Secondary actions stay near the related content and visually quieter.
- Destructive actions stay separated from submit/continue actions.
- Bottom action areas must be stable, safe-area-aware, and not cover important content.
- Small-screen layouts must not clip, overlap, jump, or force awkward button wrapping.

Baseline proof:
- scripts/ralph/miniprogram_visual_polish_proof.sh is now the visual polish proof runner.
- Every later story must run scripts/ralph/miniprogram_visual_polish_proof.sh and scripts/ralph/miniprogram_upload_stability_proof.sh.

Status:
- Archived completed upload stability PRD to scripts/ralph/archive/miniprogram_upload_2_stability_prd_20260503.json.
- Archived completed upload stability progress to scripts/ralph/archive/miniprogram_upload_2_stability_progress_20260503.txt.
- Active PRD is now mini program visual refresh.
- MP-VISUAL-001 completed: added a local visual proof runner that checks parent-home, parent-bind, parent-upload, and parent-wrongbook page structure, primary/secondary action hierarchy, destructive action separation, bottom submit spacing, narrow-screen button rules, and prototype-only wording without WeChat devtools or backend services.
- MP-VISUAL-002 completed: shared visual primitives now live in miniprogram/miniprogram/app.wxss for page shells, cards, headings, primary/secondary/icon buttons, state tags, and safe-area-aware bottom action bars. Page-level wxss keeps upload crop/canvas and page-specific layout rules, but no longer owns scattered primary/secondary button definitions. The visual proof now locks this contract and the upload stability baseline still passes.
- MP-VISUAL-003 completed: parent home child cards now put the primary upload action before the secondary wrongbook action; upload box actions now group add/rotate as normal tools and isolate delete as a danger action; upload success state now makes wrongbook/progress the primary next step and return-home secondary. Visual proof and upload stability proof passed through /tmp/xingrun_mp_visual_003_proof.sh.
- MP-VISUAL-004 completed: parent home now presents Xingrun parent identity, a designed no-child empty state, bound-child count/context, child cards with class and teacher metadata, a concise progress hint, and narrow-screen wrapping rules for long names and metadata. Visual proof and upload stability proof passed through /tmp/xingrun_mp_visual_004_behavior_proof.sh before marking the story complete.
- MP-VISUAL-005 completed: parent-bind now has an official invite lookup panel, structured already-bound child cards, class result cards, per-student bind confirmation cards, a designed no-students state, and narrow-screen wrapping rules for long class/student text. Visual proof and upload stability proof passed through /tmp/xingrun_mp_visual_005_proof.sh before marking the story complete.
- Next story: MP-VISUAL-006 Polish wrong question upload page.
