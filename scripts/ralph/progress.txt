# Ralph Progress Log
Started: 2026-05-02
---

Current PRD: website frontend stability hardening.

Scope:
- Website frontend only: frontend/ Vite + React workspace.
- Do not include WeChat mini program stability in this PRD.
- Do not include backend worker reliability or production data repair in this PRD.
- Each iteration should complete one user story from scripts/ralph/prd.json.

Baseline proof for every completed story:
- npm --prefix frontend test
- npm --prefix frontend run build
- git diff --check

Status:
- FE-STAB-001 passes: added scripts/ralph/frontend_stability_proof.sh as the shared frontend baseline proof runner.
- FE-STAB-002 passes: App bootstrap auth check no longer triggers a full reload for stale tokens; storage guard coverage now includes blocked storage, stale token removal failure, and direct storage-read source guards.
- FE-STAB-003 passes: App course calendar API failures now clear loading, show a page-level load error for list refresh failures, and alert on schedule/custom schedule save or delete failures; course-calendar tests cover list load failure, save failure, and delete/refresh failure paths.
- FE-STAB-004 passes: App workspace navigation now resolves disallowed selected tabs through a shared dashboard fallback, uses the guarded navigation setter for sidebar/dashboard shortcuts, and source assertions cover owner/admin/member/staff plus unauthenticated paths.
- Added scripts/ralph/run_codex_ralph.sh for optional Codex CLI auto-loop execution; --check and --print-prompt modes validate the next story without invoking Codex.
- Upgraded the local Codex CLI from 0.118.0 to 0.128.0 after 0.118.0 rejected gpt-5.5; scripts/ralph/run_codex_ralph.sh now defaults to gpt-5.5 again.
- Next story: FE-STAB-005.
